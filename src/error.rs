/// This error contains all predictable types of failures
/// that can occur during the token generation/signing and requesting.
pub enum TokenGenerationError {
    // TODO define types

    // Genration/Creation and other

    // RSA SHA256 / Crypto Errors

    // Reqwest errors
}
